// Encoded secret fixture — plaintext assignment is only inside the base64 payload
// (payload decodes to password=<secret>; do not put plaintext assignment in comments)
const payload = "cGFzc3dvcmQ9U3VwZXJTZWNyZXQxMjM=";
